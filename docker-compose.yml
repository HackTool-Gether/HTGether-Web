services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: http://localhost:4000/api
    ports:
      - "3000:3000"
    environment:
      HOSTNAME: "0.0.0.0"
    networks:
      - htgether

networks:
  htgether:
    name: htgether
    driver: bridge
